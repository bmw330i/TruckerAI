{
  "name": "db-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server to persist and query JSON documents for trucking loads",
  "scripts": {
    "start": "node index.mjs",
    "dev": "node index.mjs",
    "install": "npm install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.1.0",
    "zod": "^3.23.8"
  }
}
